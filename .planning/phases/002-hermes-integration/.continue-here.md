---
phase: 002-hermes-integration
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-08-20T00:14:17.375Z
---

<current_state>
A migração das rotinas do Assistente Pessoal V2 para o bot `HermesAgentAssistente` foi implementada e publicada em `origin/master`. A primeira ativação na VPS foi concluída com sucesso: o listener do bot antigo `Assistente Pessoal` foi desativado, o gateway Hermes reiniciou e passou a entregar as rotinas do V2. Depois foi identificado que o segundo cérebro não havia sido incluído na ponte MCP do Hermes. A correção também foi implementada, testada e enviada ao GitHub no commit `38826aa`. Falta atualizar novamente a VPS e validar o fluxo pelo Telegram.
</current_state>

<completed_work>

- Task 1: enriquecer compras do Mercado Livre e Shopee usando os e-mails do usuário - concluída e publicada anteriormente no commit `967723f`.
- Task 2: migrar a entrega das rotinas do Telegram para o novo bot Hermes - concluída no commit `3f2320d`; o script `scripts/activate-hermes-telegram.sh` foi executado com sucesso na VPS em 2026-08-19.
- Task 2: desativar o listener do bot antigo e reiniciar o gateway Hermes - concluída; a saída da VPS confirmou a migração e criou backups de `.env` e `~/.hermes/config.yaml`.
- Task 2: expor no Hermes as ferramentas de hábitos, tarefas vencidas e listas de viagem - concluída e ativada na VPS.
- Task 3: diagnosticar a falha ao salvar um resumo de podcast no segundo cérebro - concluída. A causa foi a ausência das ferramentas de conhecimento na ponte MCP e no `tools.include` do Hermes; por isso ele usou a skill genérica `obsidian`, procurou um vault local e escreveu um Markdown fora do fluxo do V2.
- Task 3: criar `knowledge_save_content`, `knowledge_save_url` e `knowledge_search` na ponte MCP - concluída no commit `38826aa`.
- Task 3: instruir o Hermes a usar o segundo cérebro real do V2, nunca procurar outro vault pelo terminal e só confirmar salvamento após releitura com `verified: true` - concluída.
- Task 3: validar a correção - concluída localmente; typecheck e build passaram, e 12 testes de MCP passaram.
- Task 3: publicar a correção - concluída; `master` foi enviada ao GitHub em `38826aa`.
</completed_work>

<remaining_work>

- Atualizar a VPS com `git pull origin master`.
- Executar novamente `bash scripts/activate-hermes-telegram.sh` para reconstruir o V2, acrescentar as três ferramentas `knowledge_*` ao Hermes, copiar a skill atualizada e reiniciar o gateway.
- No Telegram, responder ao resumo original do podcast com “Adicione esse conteúdo ao meu segundo cérebro”. O Hermes deve chamar `knowledge_save_content`, sem usar a skill genérica `obsidian` e sem perguntar o caminho do vault.
- Confirmar que a resposta da ferramenta contém `verified: true` e informa uma nota no formato `[[nome-da-nota]]`.
- Fazer uma consulta de validação, por exemplo “O que tenho salvo sobre parar de ser medíocre?”, e confirmar que `knowledge_search` encontra a nova nota.
- O resumo solicitado antes da correção provavelmente não entrou em `/root/assistente-vault/Sources`; após a atualização, reenviar ou responder ao resumo original para salvá-lo corretamente.
</remaining_work>

<decisions_made>

- O Hermes continua sendo a interface conversacional única no Telegram; o bot antigo permanece com o listener desativado.
- O Assistente Pessoal V2 continua sendo a fonte de verdade para finanças, hábitos, tarefas, listas e segundo cérebro, acessado pelo Hermes via MCP autenticado.
- Texto já produzido na conversa usa `knowledge_save_content`; links ainda não processados usam `knowledge_save_url`; consultas usam `knowledge_search`.
- O vault verdadeiro continua montado no serviço do V2 em `/vault`, correspondente a `/root/assistente-vault` no host da VPS.
- A nota Markdown é a fonte de verdade. Uma falha de embedding não perde a nota: o salvamento pode retornar `verified: true` e `indexed: false`, deixando a reindexação para depois.
- O Hermes só deve afirmar que salvou depois que a ferramenta reler o arquivo e retornar `verified: true`.
- As ferramentas do segundo cérebro são exclusivas do privado do Luis; não devem ser usadas no privado da esposa nem em grupos.
</decisions_made>

<blockers>

- Nenhum bloqueio de código conhecido.
- A validação final depende de executar a atualização na VPS e testar no Telegram.
- Existem cinco arquivos não rastreados em `.planning/debug/` que já estavam no worktree e não foram incluídos nos commits. Preservá-los; não apagar nem sobrescrever sem revisão específica.
</blockers>

<context>
Commits relevantes, em ordem: `967723f` (identificação de compras por e-mail), `3f2320d` (migração do Telegram para Hermes) e `38826aa` (segundo cérebro no Hermes). A ativação anterior na VPS funcionou antes do último commit, portanto precisa ser repetida. O aviso local sobre um caminho antigo do `conda.exe` no perfil do PowerShell não afetou commit, testes ou push. Não há necessidade de refazer a implementação nem modificar tokens. Não registrar segredos neste handoff.
</context>

<next_action>
Na VPS, executar exatamente: `cd /root/assistente-pessoal-v2`, depois `git pull origin master` e `bash scripts/activate-hermes-telegram.sh`. Em seguida realizar o teste do resumo no Telegram e verificar o salvamento no segundo cérebro.
</next_action>

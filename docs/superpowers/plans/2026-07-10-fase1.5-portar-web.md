# Fase 1.5 — Portar o web app da v1: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** O dashboard de finanças da v1 (React+Vite+Recharts) servido pela v2, sobre as mesmas tabelas do Supabase compartilhado, sem as páginas do bot da v1.

**Architecture:** Copiar `apps/web` da v1 para o monorepo v2 e aparar o que depende das tabelas dropadas (Compras/Tarefas/Rotinas) e do endpoint `/api/sync-bank` (volta na Fase 3). Adicionar servidor HTTP (Hono) ao serviço v2 para servir o build estático do SPA, ao lado do bot e do scheduler.

**Tech Stack:** React 19 + Vite 6 + Tailwind v4 + Recharts (herdados da v1); hono + @hono/node-server no servidor.

## Global Constraints

- Copy voltada ao usuário em PT-BR; identificadores em inglês.
- ESM NodeNext no server → imports relativos com `.js`.
- Não redesenhar o SPA nesta fase: porte fiel + remoção do que quebrou. Melhorias ficam no backlog da spec.
- `npm test` e `npm run typecheck` do server continuam verdes; `npm run web:build` limpo.
- Segredos só em `.env`/`apps/web/.env` (gitignored; `.dockerignore` exclui apenas o `.env` da raiz — o de `apps/web` PRECISA entrar no contexto de build do Docker porque o Vite embute as VITE_* no build).

## Fonte

v1 em `C:\Users\LUIS BARBOSA\.claude\projects\assistente-pessoal\apps\web` (copiar `src/`, `index.html`, `package.json`, `vite.config.ts`, `tsconfig*.json`, `public/` se existir, `.env`, `.env.example`; NUNCA copiar `node_modules/`, `dist/`, `.vite/`).

---

### Task 1: Copiar e aparar o SPA

**Files:**
- Create: `apps/web/**` (cópia da v1)
- Modify: `apps/web/src/App.tsx` (rotas), `apps/web/src/components/Layout.tsx` (ou onde estiver a sidebar), `apps/web/src/pages/Transacoes.tsx` (botão de sync), `package.json` raiz (scripts web:dev/web:build), `vitest.config.ts` se necessário (excluir testes do web do runner raiz, que usam outro setup)

**Passos:**
- [ ] Copiar os arquivos da fonte (lista acima) para `apps/web/` na v2.
- [ ] Remover páginas e referências: rotas `/compras`, `/tarefas`, `/rotinas` em `App.tsx`; links correspondentes na sidebar; deletar `src/pages/{Compras,Tarefas,Rotinas}.tsx` e helpers usados só por elas (verificar imports órfãos).
- [ ] Em Transações: remover o botão "Atualizar dados"/sync-bank e o fetch para `/api/sync-bank` (comentário: volta na Fase 3).
- [ ] Adicionar ao `package.json` raiz: `"web:dev": "npm run dev -w apps/web"`, `"web:build": "npm run build -w apps/web"`.
- [ ] `npm install` na raiz (workspace novo) → `npm run web:build` limpo → `npm test && npm run typecheck` (server) continuam verdes. Se o vitest raiz começar a captar testes do web (`finance-data.test.ts`), incluir `apps/web` no runner só se passarem sem setup extra; senão restringir o include ao server e anotar no report.
- [ ] Commit: `feat: porta o web app de finanças da v1 (fase 1.5)`

### Task 2: Servidor HTTP servindo o SPA

**Files:**
- Create: `apps/server/src/api/server.ts`
- Modify: `apps/server/src/lib/config.ts` (adicionar `PORT: z.coerce.number().default(8080)`), `apps/server/src/index.ts` (iniciar o servidor http), `.env.example` (PORT)

**Interfaces:**
- Produces: `startWebServer(cfg: Config): void` — Hono + `@hono/node-server`; `GET /health` → `{ ok: true }`; estáticos de `apps/web/dist` com fallback SPA para `index.html`; se `apps/web/dist` não existir, loga aviso e não sobe (bot segue normal).

**Passos:**
- [ ] `npm i -w apps/server hono @hono/node-server`
- [ ] Implementar `api/server.ts` (serveStatic + fallback + /health; caminho do dist resolvido relativo ao repo root, funcionando em dev e no Docker).
- [ ] Wire em `index.ts` após `startScheduler()`.
- [ ] Teste unitário do que for puro (ex.: resolução do caminho/decisão de subir ou não) se factível sem subir servidor; senão gate = typecheck + smoke manual (`curl localhost:8080/health`).
- [ ] `npm test && npm run typecheck && npm run build` verdes.
- [ ] Commit: `feat: servidor http (hono) servindo o dashboard`

### Task 3: Docker e deploy

**Files:**
- Modify: `Dockerfile` (copiar `apps/web/package.json` no ci, copiar `apps/web` e `apps/web/.env`, rodar `npm run web:build`, copiar `apps/web/dist` para o runtime), `docker-compose.yml` (ports "8080:8080"), `DEPLOY.md` (copiar também `apps/web/.env` para o servidor)

**Passos:**
- [ ] Atualizar os três arquivos.
- [ ] Conferir `.dockerignore`: `**/dist` já exclui dist local (build refaz); garantir que `apps/web/.env` NÃO está excluído (regra atual `.env` cobre só a raiz — manter assim).
- [ ] Se Docker local disponível, `docker build .` de sanidade; senão anotar.
- [ ] Commit: `feat: build do web no Docker e porta 8080`

### Task 4: Smoke local

- [ ] `npm run web:build && npm run build && node apps/server/dist/index.js` com `.env` real: `/health` responde, `/` serve o dashboard (login aparece). Bot NÃO deve conflitar com a v1 se ela ainda estiver ligada — se estiver, rodar o smoke por no máximo alguns segundos ou com a v1 já desligada.
- [ ] UAT (humano, pós-SQL e pós-desligamento da v1): login funciona, dashboard mostra junho/2026 correto (KPIs, categoria vs meta), Transações lista as 167, Categorias/Objetivos/Compromissos intactos.
